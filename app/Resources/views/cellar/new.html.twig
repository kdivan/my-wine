{% extends 'AppBundle::layout.html.twig' %}

{% block body %}
<div class="content">
    <h1>Cellar <span class="red">creation</span></h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <div class="line">
        <p class="back">
            <a href="{{ path('cellar_index') }}">Back to the list</a>
        </p>
    </div>
</div>

{% endblock %}
