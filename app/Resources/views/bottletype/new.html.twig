{% extends 'AppBundle::layout.html.twig' %}

{% block body %}
    <h1>{{ 'bottle_type.creation'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('bottletype_index') }}">{{ 'back_to_list'|trans }}</a>
        </li>
    </ul>
{% endblock %}
